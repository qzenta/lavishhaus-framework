# packages/seo (Track 2 — not yet extracted)

Extraction target for `apps/lavishhaus/lib/structured-data.ts` (Organization,
LocalBusiness/FurnitureStore, Breadcrumb, FAQ, Product JSON-LD builders) and the
per-page Metadata API patterns used across `apps/lavishhaus/app/**/page.tsx`.
