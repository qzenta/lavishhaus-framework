# packages/analytics (Track 2 — not yet extracted, not yet implemented)

Not wired up in Track 1. When GA4/Clarity (or equivalent) are added to
`apps/lavishhaus`, extract the wiring here so future catalogue clients get
consistent analytics setup — matching the pattern already used across other
Qzenta client sites.
