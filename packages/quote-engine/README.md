# packages/quote-engine (Track 2 — not yet extracted)

Extraction target for `apps/lavishhaus/services/quote-service.ts` and
`contact-service.ts` (Brevo submission logic) plus the future n8n webhook step
referenced in the master prompt's quote workflow: Collection → Request Quote →
Form → Brevo → n8n.
