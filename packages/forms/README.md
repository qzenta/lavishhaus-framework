# packages/forms (Track 2 — not yet extracted)

Extraction target for `apps/lavishhaus/types/forms.ts` (Zod schemas) and the
React Hook Form + shadcn form components in `apps/lavishhaus/components/shared`
(`quote-form.tsx`, `contact-form.tsx`).
