// Regenerate with `npm run generate:importmap` once custom admin
// components are registered in payload.config.ts. Empty is valid for a
// config with no custom components.
export const importMap = {};
